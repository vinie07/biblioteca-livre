/******************************************************************************
 *  TITULO: 
 *  DESCRICAO:  
 *
 *
 * 
 *  AUTOR:
 *  EMAIL: @
 *  DATA: //2026
 ******************************************************************************/
/* === BIBLIOTECAS === */
#include <stdio.h>   //biblioteca basica
#include <stdlib.h>  // SRAND(time(NULL))
#include <time.h>    // srand(TIME(NULL))
#include <string.h>  // gets,puts,strcpy(),strcat(),strcmp(),strlen,
#include <math.h>    // pow(,2) , sqrt()

/* === DEFINE === */
#define x "texto"

/* === FUNCAO PRINCIPAL === */
int main(){ 
    /* === VARIAVEIS === */	
    /* --- int (inteiros) --- */
	
    /* --- float (decimais) --- */
	
    /* --- char (caractere) --- */
	
    /* --- char[] (strings/texto) --- */

	/* --- contadores e auxiliares --- */

	/* === ENTRADA DE DADOS === */


	/* === PROCESSAMENTO === */	


    /* === SAIDA DE DADOS === */
	
	/* --- comentarios...  --- */
    return 0;
}
